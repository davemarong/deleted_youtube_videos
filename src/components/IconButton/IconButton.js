/* eslint-disable no-unused-expressions */
/* eslint-disable no-undef */
// IMPORT

// React

// Material UI
import IconButtonMUI from "@mui/material/IconButton";

// Components

// Custom Hook

// Utils

// External

// Data

// Functional component
export const IconButton = ({ children, func }) => {
  // State

  // Functions

  // Return
  return <IconButtonMUI onClick={func}>{children}</IconButtonMUI>;
};
